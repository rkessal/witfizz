version: "3.8"

services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "81:81"
    environment:
      - NODE_ENV=production
    env_file:
      - ./client/.env
    depends_on:
      - backend
    networks:
      - app-network

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5002:5002"
      - "5080:5080"
    environment:
      - NODE_ENV=production
      - DB_HOST=db
    env_file:
      - ./server/.env
    depends_on:
      - db
    networks:
      - app-network

  db:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    env_file:
      - ./server/.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./server/src/db/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./server/src/db/schema/create.sql:/docker-entrypoint-initdb.d/02-create.sql
      - ./server/src/db/schema/development.sql:/docker-entrypoint-initdb.d/03-development.sql
      - ./server/src/db/schema/production.sql:/docker-entrypoint-initdb.d/04-production.sql
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
